import React from "react";
import PageLayout from "../layouts/pageLayout";

const Dashboard = () => {
    const content = <div className="">dashboard</div>;

    return <PageLayout content={content} />;
};

export default Dashboard;
